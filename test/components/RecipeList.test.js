/*import React from 'react'
import { createRenderer } from 'react-addons-test-utils'

import expect from 'expect'
import expectJSX from 'expect-jsx'
import RecipeList from '../../src/components/RecipeList'

expect.extend(expectJSX)

describe('components', () => {
  
  describe('RecipeList component', () => {

    // Render List of Recipes
    // ********************************

    it('should display the recipe list', () => {

      // code to stub list
      let list = []

      // code to create render object to test
      let renderer = createRenderer()
      renderer.render(<RecipeList list={list} />)
      let actualOutput = renderer.getRenderOutput()

      let expectedOutput = <div style={{flex : '1 30%'}} />

      expect(actualOutput).toIncludeJSX(expectedOutput)

    })

  })



})
*/